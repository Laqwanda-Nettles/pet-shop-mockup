import PetAboutText from './PetAboutText';

const meta = {
  component: PetAboutText,
};

export default meta;

export const Default = {
  args: {
    title: "Um app não, uma caixinha de amigos..."
  }
};

export const English = {
  args: {
    title: "Not an app,  a little box of friends..."
  }
};