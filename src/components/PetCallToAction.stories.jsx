import PetCallToAction from './PetCallToAction';

const meta = {
  component: PetCallToAction,
};

export default meta;

export const Default = {
  args: {
    ctaText: "Call To Action Text"
  }
};

export const AccessNow = {
  args: {
    ctaText: "Access Now!"
  }
};